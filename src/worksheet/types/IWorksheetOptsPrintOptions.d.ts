export interface IWorksheetOptsPrintOptions {
  centerHorizontal: boolean; // Should data be centered horizontally when printed
  centerVertical: boolean; //  Should data be centered vertically when printed
  printGridLines: boolean; //  Should gridlines by printed
  printHeadings: boolean; //  Should Heading be printed
}
